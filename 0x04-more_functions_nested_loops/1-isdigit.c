#include "main.h"
#include <stdio.h>

/**
  *_isdigit - checks if int is a digit
  *@c: int to be checked
  *Return: 0
  */

int _isdigit(int c)
{
	for (c = 1 ; c <= 9 ; c++)
	{
		return (1);
	}
	return (0);
}
