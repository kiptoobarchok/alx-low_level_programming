#include <stdio.h>
#include "main.h"

/**
  *mul - function to perform multiplication
  *@a: 1st number
  *@b: 2nd number
  *Return: result
  */

int mul(int a, int b)
{
	int result;

	result = (a * b);

	return (result);
}
