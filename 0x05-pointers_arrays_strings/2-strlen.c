#include <stdio.h>
#include "main.h"
#include <string.h>

/**
  *_strlen - function to return length of the string
  *@s: string whose length is to be printed
  *Return: length of string
  */

int _strlen(char *s)
{
	int length = strlen(s);

	return(length);
}
